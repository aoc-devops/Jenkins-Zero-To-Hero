pipeline {
    agent any
    environment {
        DOCKER_CREDS = credentials('docker_creds')
    }
    stages {
        stage('docker_login'){
            steps {
                sh 'docker login -u ${DOCKER_CREDS_USR} -p ${DOCKER_CREDS_PSW}'
                sh 'echo $?'
            }

        }
    }
}